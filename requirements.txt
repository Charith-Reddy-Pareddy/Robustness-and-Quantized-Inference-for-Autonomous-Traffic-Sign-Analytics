torch==2.10.0
torchvision==0.25.0
numpy==2.2.6
pandas==2.2.3
scikit-learn==1.5.2
# scipy 1.16+ requires Python >=3.11; CI (and this project's documented env) pins Python
# 3.10, so this stays on the 1.15.x line. On some macOS arm64 Python builds, PyPI's 1.15.x
# wheels fail to import (a dyld/_propack incompatibility unrelated to this project and
# unrelated to CI, which runs on Linux) -- if that happens locally, use a Python 3.10
# install that doesn't hit it, or a local venv override; don't bump this pin for it.
scipy==1.15.3
matplotlib==3.10.0
seaborn==0.13.2
opencv-python==5.0.0.93
onnx==1.22.0
onnxruntime==1.23.2
grad-cam==1.5.5
kaggle==1.7.4.5
pytest==8.3.3
tqdm==4.66.5
Pillow==12.0.0
